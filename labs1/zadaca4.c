#include <stdio.h>
#include <stdlib.h>

int zadaca4()
{
    int n,sum;

    int a[];

    scanf("%d", &n);

    for (int i = 0; i < n; ++i) {
        scanf("%d", &a[i]);
    }

    for (int i = 0; i < n; ++i) {
        sum+=a[i]
    }
    if(n%2==0 || n%2!=0)

        while(n){
            zbir=n%10;
            zbir+=cifra;
            n/10;
        }

    printf("%.2f", sum/n*1.0);
}
